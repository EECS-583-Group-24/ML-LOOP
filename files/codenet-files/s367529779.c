#include<stdio.h>

int main() {
  int W, H, x, y;
  scanf("%d %d %d %d", &W, &H, &x, &y);
  int i;
  if (W / 2 == a && H / 2 == y) {
    i = 1;
  } else {
    i = 0;
  }
  
  printf("%d %d\n", (W * H) / 2, i);
}