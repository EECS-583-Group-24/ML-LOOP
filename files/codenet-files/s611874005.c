#include <stdio.h>

int heikan(int n);

int main(void)
{
      int n;
      scanf("%d", &n);
      heikin(n);
      printf("%d\n", n);
      return 0;
}

int heikin(int n)
{
      n=1110-n;
      return 0;
}
