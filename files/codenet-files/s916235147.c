#include <stdio.h> 
int main(void){
  int i, j, tmp;
  /* ??°??????????´????????????? */
  int number[100];
  /* ??°???????????°?????\??? */
  int total;
  scanf("%d", &total);
  /* ?????????????´?????????°????????\??? */
  for (i=0; i<total; ++i){
    scanf("%d", &number[i]);
  }
  /* ??°???????????????????????? */
  for (i=0; i<total; ++i) {
    for (j=i+1; j<total; ++j) {
      if (number[i] > number[j]) {
        tmp =  number[j];
        number[j] = number[i];
        number[i] = tmp;
      }
    }
  }
  /* ???????????????????????°???????????? */
  for (i=0; i<total; ++i){
    printf("%d ", number[i]);
  }
  return 0;
}