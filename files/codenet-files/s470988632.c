#include<stdio.h>

int main(void){
    int h, w, x, y;
    if(scanf("%d %d %d %d", &h, &w, &x, &y));
    printf("%lf %d\n", (double)h * w / 2.0, (double)x == w / 2.0 && (double)y == h / 2.0);
    return 0;
}